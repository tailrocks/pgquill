# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "tailrocks/pgquill"
profile = "generic"
verified = true
default_branch = "main"
runners = "both"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = []
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs."]

[workflow]
github_runner = "ubuntu-24.04"
velnor_labels = ["self-hosted", "velnor-target-mvp"]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-docs.yml", "maintenance.yml", "nightly.yml"]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "docs-links"
label = "Docs link check"
kind = "docs"
root = "."
watch = ["FUTURE_SQL.md", "README.md"]
github_pr_commands = ["mise run links"]
github_full_commands = ["mise run links"]
velnor_pr_commands = ["mise run links"]
velnor_full_commands = ["mise run links"]
workflow_file = "ci-unit-docs.yml"
